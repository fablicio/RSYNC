name: MyApp
 on:
  workflow_call:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses:   github.com/fablicio/pipeline-templates/blob/master/.github/workflows/develop.yml
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
