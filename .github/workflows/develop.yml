name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: /fablicio/RSYNC/.github/workflows/develop.yml@master
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
