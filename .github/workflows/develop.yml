name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: /pipeline-templates/tree/master/.github/workflows/develop.yml@main
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
