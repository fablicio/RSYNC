name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: ./.github/workflows/deploydevelop.yml@v1
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
