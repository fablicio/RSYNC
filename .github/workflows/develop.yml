name: MyApp
 on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses:   .github/workflows/develop.yml@main
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
