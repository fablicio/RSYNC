name: MyApp

on:
  push:
    branches:
      - develop

jobs:
  CallReusablePipeline:
    uses: ./pipeline-templates/.github/workflows/reusable-pipeline.yml
    with:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
      image_name: "fabliciosoares/myapp"
    secrets: inherit
