name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: fablicio/pipeline-templates/.github/workflows/deploydevelop.yml
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
