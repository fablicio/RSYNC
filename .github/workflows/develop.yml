name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: github.com/fablicio/pipeline-templates/blob/master/.github/workflows/deploydevelop.yml@master
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
