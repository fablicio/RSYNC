name: MyApp

on:
  push:
    branches:
      - develop

jobs:
  CallReusablePipeline:
    uses: ../.github/workflows/reusable-pipeline.yml
    with:
      project: "myapp_develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
