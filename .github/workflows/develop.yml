name: MyApp

on:
  push:
    branches:
      - develop

jobs:
  CallReusablePipeline:
    uses: github.com/fablicio/pipeline-templates/.github/workflows/reusable-pipeline.yml
    with:
      project: "myapp_develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
