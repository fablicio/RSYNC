name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: github.com/fablicio/pipeline-templates/blob/master/.github/workflows/deploydevelop.yml
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
