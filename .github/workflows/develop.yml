name: MyApp
on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses: .github/workflows/deploydevelop.yml
    with:
      project: "myapp_develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
