name: MyApp
 on:
  push:
    branches:
      - develop

jobs:
  CallTemplate:
    uses:  github.com/fablicio/pipeline-templates/actions/workflows/develop.yml@main
    with:
      project: "MyApp develop"
      dockerfile: "Dockerfile"
      environment: "dev"
    secrets: inherit
